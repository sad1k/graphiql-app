import { PINK_COLOR, WHITE_COLOR } from '@/constants/colors';

const style = {
  container: { backgroundColor: PINK_COLOR, p: '4rem 0' },
  title: { color: WHITE_COLOR, mb: 4, textAlign: 'center' },
};

export default style;
